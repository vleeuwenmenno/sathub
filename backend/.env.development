# Database Configuration
DB_PATH=./satdump.db

# Server Configuration
PORT=4001
GIN_MODE=debug

# SMTP Configuration for Development (Mailpit)
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@satdump.local
SMTP_FROM_NAME=SatDump

# JWT Configuration
JWT_ACCESS_SECRET=dev-access-secret-key-change-in-production
JWT_REFRESH_SECRET=dev-refresh-secret-key-change-in-production